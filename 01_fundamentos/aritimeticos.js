const [a, b, c, d] = [1, 4, 8, 2];

const soma = a + b + c + d;
const subtracao = d - c;
const multiplicacao = c * a;
const divisao = b / d;
const modulo = c % 2;

console.log(soma, subtracao, divisao, multiplicacao, modulo);
